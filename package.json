{
  "name": "auto-agent-router",
  "version": "1.0.0",
  "description": "Auto Agent Router — 181 agents, auto-decompose prompts, dispatch subagents in Claude Code",
  "bin": {
    "agr": "./dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["claude", "agent", "claude-code", "multi-agent", "agent-teams", "ai"],
  "license": "MIT",
  "files": ["dist/", "agents/", "templates/"],
  "dependencies": {
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
